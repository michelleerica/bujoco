<p id="notice"><%= notice %></p>

<!-- <p>
  <strong>Image:</strong>
  <%= @design.image %>
</p> -->

<canvas id="mainCanvas" width="1000" height="600"></canvas>


  <strong>User:</strong>
  <%= @design.user_id %>
</p>


<div class='hidden'>
  <p>
    <strong>Elements:</strong>
  <%@design.elements.each_with_index do |e, i|%>
    <div class="flourish">



    <!-- <#%=raise "hell"%> -->
    <div id="name<%=i%>"><%= e.flourish.name%></div>
    <div id="left<%=i%>"><%= e.left%></div>
    <div id="top<%=i%>"><%= e.top%></div>
    <div id="width<%=i%>"><%= e.width%></div>
    <div id="height<%=i%>"><%= e.height%></div>
    <div id="angle<%=i%>"><%= e.angle%></div>

  </div>
  <%end%>
</p>
</div>
<%= link_to 'Edit', edit_design_path(@design) %> |
<%= link_to 'Back', designs_path %>
